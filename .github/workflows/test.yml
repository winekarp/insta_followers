name: Run Tests

on:
  workflow_dispatch:  # Manual trigger

jobs:
  wdio:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '20'  # Set Node.js version to 20

      - name: Install dependencies
        run: npm install

      - name: Run WDIO tests
        env:
          USER_NAME: ${{ secrets.USER_NAME }}
          USER_PASSWORD: ${{ secrets.USER_PASSWORD }}
          FILE_WITH_NAMES: ${{ secrets.FILE_WITH_NAMES }}
        run: |
          npm run wdio user_name=blokha_baba123 user_password=blokha_babablokha_baba file_with_names=https://www.editpad.org/?edit-id=qs9nfQUNbidfc7e22e
